# dream-test: dir

def main():
    let prefix = "hello"
    let literal_result = "dream " + "language"
    let variable_result = prefix + " world"
    let nested_result = "[" + prefix + "]"
    print(literal_result)
    print(variable_result)
    print(nested_result)
