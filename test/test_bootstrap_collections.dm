let numbers: list[int] = [1, 2, 3, 4]
let selected = [value for value in numbers if value > 1]
let pair = (10, 20)
let (left, right) = pair

print(len(selected))
print(selected[0])
print(left + right)
print(-1 + 2)
