# Dream 语言自举路线图

## 自举目标
用 Dream 语言重新实现 Dream 编译器，实现自举（self-hosting）。当前编译器用 OCaml 实现，目标是用 Dream 完全替代。

---

## 前置条件检查

### 已具备的基础
- [x] 基础类型系统（int, float, string, bool）
- [x] 函数定义和调用
- [x] 控制流（if/else, while, for）
- [x] 数组操作（索引、切片、拼接、列表推导式）
- [x] 动态内存管理（引用计数 + 分代GC）
- [x] 递归函数支持
- [x] LLVM IR 代码生成

### 待实现功能（按优先级排序）

---

## 第一阶段：底层基础设施（P0 - 核心依赖）

### 1. 文件 I/O 系统
**为什么需要**：编译器需要读取源文件、写入生成的代码
- [ ] 文件读取函数 `read_file(path: string) -> string | Error`
- [ ] 文件写入函数 `write_file(path: string, content: string) -> bool | Error`
- [ ] 文件系统函数 `exists(path: string) -> bool`
- [ ] 目录操作 `list_dir(path: string) -> list[string]`
- [ ] 路径操作 `join_path(parts: list[string]) -> string`

**依赖**：
- 需要 C FFI 或系统调用接口
- 错误处理机制（Result 类型）

### 2. 字符串操作增强
**为什么需要**：词法分析和语法分析大量依赖字符串处理
- [ ] 字符串比较 `==`, `!=`, `<`, `>`
- [ ] 字符串拼接 `+` 运算符（已有，需验证）
- [ ] 字符串长度 `len(s: string) -> int`
- [ ] 字符串索引 `s[i]` 返回字符
- [ ] 字符串切片 `s[start:end]`
- [ ] 子串查找 `find(s: string, sub: string) -> int | None`
- [ ] 字符串分割 `split(s: string, sep: string) -> list[string]`
- [ ] 字符串连接 `join(parts: list[string], sep: string) -> string`
- [ ] 去除空白 `strip(s: string) -> string`
- [ ] 大小写转换 `upper(s: string)`, `lower(s: string)`
- [ ] 字符串替换 `replace(s: string, old: string, new: string) -> string`
- [ ] 判断前缀/后缀 `starts_with()`, `ends_with()`
- [ ] 字符类型判断 `is_digit()`, `is_alpha()`, `is_whitespace()`

**依赖**：
- 字符类型（char）
- 字符串内部实现优化

### 3. 错误处理机制
**为什么需要**：编译器需要优雅地处理各种错误情况
- [ ] Result 类型 `Result[T, E] = Ok(T) | Err(E)`
- [ ] Option 类型完善 `Option[T] = Some(T) | None`
- [ ] 错误类型定义 `Error` 类型
- [ ] 错误传播语法 `?`
- [ ] 自定义错误类型

**依赖**：
- 联合类型（部分已有）
- 模式匹配

### 4. 字典类型实现
**为什么需要**：符号表、环境管理、类型环境都需要字典
- [ ] 字典字面量 `{"key": value}`
- [ ] 字典索引 `dict[key]`
- [ ] 字典赋值 `dict[key] = value`
- [ ] 字典方法：
  - [ ] `get(key) -> T | None`
  - [ ] `set(key, value)`
  - [ ] `has(key) -> bool`
  - [ ] `remove(key)`
  - [ ] `keys() -> list[K]`
  - [ ] `values() -> list[V]`
  - [ ] `items() -> list[(K, V)]`
- [ ] 字典迭代 `for k, v in dict.items()`
- [ ] 哈希表底层实现

**依赖**：
- 元组类型
- 泛型支持
- 哈希函数

### 5. 元组类型实现
**为什么需要**：多返回值、模式匹配、字典 items 等都需要元组
- [ ] 元组字面量 `(1, "two", 3.0)`
- [ ] 元组索引 `tuple[0]`
- [ ] 元组解包 `let (a, b, c) = tuple`
- [ ] 元组比较
- [ ] 嵌套元组支持

**依赖**：
- 基础类型系统扩展
- 模式匹配

---

## 第二阶段：语言核心特性（P1 - 编译器必需）

### 6. 模式匹配
**为什么需要**：AST 处理、错误处理、类型检查都大量依赖模式匹配
- [ ] match 表达式基础语法
- [ ] 值匹配 `case 42:`
- [ ] 类型匹配 `case x: int:`
- [ ] 元组解构 `case (a, b):`
- [ ] 列表解构 `case [x, ...rest]:`
- [ ] 通配符 `case _:`
- [ ] 守卫条件 `case x if x > 0:`
- [ ] 穷尽性检查（编译器警告）

**依赖**：
- 元组类型
- 联合类型
- Result/Option 类型

### 7. 枚举类型（Enum/Sum Types）
**为什么需要**：Token、AST 节点、类型定义都是枚举类型
- [ ] 枚举定义语法 `enum Token { INT(int), STRING(string), PLUS, ... }`
- [ ] 枚举构造器
- [ ] 枚举模式匹配
- [ ] 带数据的枚举变体
- [ ] 递归枚举（AST 需要）

**依赖**：
- 联合类型的完整实现
- 模式匹配

### 8. 泛型系统
**为什么需要**：容器类型、Result/Option、通用算法都需要泛型
- [ ] 函数泛型 `def map[T, U](f: (T) -> U, list: list[T]) -> list[U]`
- [ ] 类泛型 `class Box[T] { value: T }`
- [ ] 接口泛型 `interface Comparable[T] { def compare(self, other: T) -> int }`
- [ ] 类型参数约束
- [ ] 单态化代码生成（为每个具体类型生成代码）

**依赖**：
- 完善的类型系统
- 类型推导增强

### 9. 结构体/记录类型
**为什么需要**：AST 节点、Token、Position 等需要结构体
- [ ] 结构体定义 `struct Position { line: int, column: int }`
- [ ] 结构体字面量 `Position { line: 1, column: 0 }`
- [ ] 字段访问 `pos.line`
- [ ] 字段更新 `pos.line = 2`
- [ ] 结构体方法
- [ ] 结构体比较

**依赖**：
- 类系统的简化版本
- 或独立的结构体实现

---

## 第三阶段：高级语言特性（P2 - 编译器增强）

### 10. 类和接口（简化版 OOP）
**为什么需要**：编译器各个阶段可以封装为类
- [ ] 类定义 `class Lexer { source: string, pos: int }`
- [ ] 构造函数 `def __init__(self, source: string)`
- [ ] 方法定义 `def next_token(self) -> Token`
- [ ] self 关键字
- [ ] 接口定义 `interface Visitor { def visit_expr(self, expr: Expr) }`
- [ ] 接口实现 `class TypeChecker implements Visitor`

**依赖**：
- 结构体
- 方法调用
- 多态

### 11. 高阶函数和闭包
**为什么需要**：函数式编程模式、Visitor 模式、回调函数
- [ ] 函数作为一等公民（可作为参数和返回值）
- [ ] Lambda 表达式 `(x) => x * 2`
- [ ] 闭包捕获外部变量
- [ ] map, filter, reduce 等高阶函数

**依赖**：
- 函数类型完善
- 闭包内存管理

### 12. 命名空间/模块系统
**为什么需要**：组织大型代码库、避免命名冲突
- [ ] 模块定义 `module Lexer { ... }`
- [ ] 模块导入 `import Lexer`
- [ ] 选择性导入 `from Lexer import tokenize`
- [ ] 模块别名 `import Lexer as Lex`
- [ ] 嵌套模块
- [ ] 模块私有/公有标识

**依赖**：
- 文件 I/O
- 路径解析
- 符号表增强

---

## 第四阶段：编译器核心模块（P3 - 自举实现）

### 13. 词法分析器（Lexer）
**用 Dream 重写**：当前 OCamllex 实现 → Dream 实现
- [ ] Token 类型定义（枚举）
- [ ] 位置信息 Position 结构体
- [ ] 字符扫描逻辑
- [ ] 缩进处理（INDENT/DEDENT）
- [ ] 字符串字面量解析
- [ ] 数字字面量解析
- [ ] 注释处理
- [ ] 错误报告

**依赖**：
- 枚举类型
- 字符串操作
- 结构体
- 错误处理

### 14. 语法分析器（Parser）
**用 Dream 重写**：当前 Menhir 实现 → 手写递归下降或 Pratt 解析器
- [ ] AST 节点定义（枚举 + 结构体）
- [ ] 表达式解析（运算符优先级）
- [ ] 语句解析
- [ ] 缩进敏感语法处理
- [ ] 错误恢复
- [ ] 错误报告（带位置信息）

**依赖**：
- 枚举类型
- 递归类型
- 模式匹配
- 错误处理

### 15. 类型检查器（Type Checker）
**用 Dream 重写**：当前 OCaml 实现 → Dream 实现
- [ ] 类型表示（枚举）
- [ ] 类型环境（字典）
- [ ] Hindley-Milner 类型推导
- [ ] 类型统一算法
- [ ] Occurs check
- [ ] 类型替换
- [ ] 类型错误报告

**依赖**：
- 枚举类型
- 字典类型
- 递归函数
- 泛型
- 错误处理

### 16. LLVM IR 生成器
**用 Dream 重写**：当前 OCaml 实现 → Dream 实现
- [ ] LLVM IR 字符串生成
- [ ] 类型映射（Dream → LLVM）
- [ ] 表达式代码生成
- [ ] 语句代码生成
- [ ] 函数代码生成
- [ ] 符号表管理
- [ ] 临时变量命名
- [ ] 标签生成

**依赖**：
- 字符串构建器（StringBuilder）
- 字典类型
- 模式匹配
- AST 遍历

### 17. 编译器驱动（Main）
**用 Dream 重写**：当前 OCaml bin/main.ml → Dream 实现
- [ ] 命令行参数解析
- [ ] 文件读写
- [ ] 编译流程编排
- [ ] 错误处理和报告
- [ ] 调用外部工具（clang）

**依赖**：
- 文件 I/O
- 字符串操作
- 错误处理
- 进程调用接口

---

## 第五阶段：标准库和工具（P4 - 生态系统）

### 18. 字符串构建器（StringBuilder）
**为什么需要**：高效构建大字符串（LLVM IR 生成）
- [ ] StringBuilder 类
- [ ] append() 方法
- [ ] to_string() 方法
- [ ] 内部缓冲区管理

### 19. 命令行参数解析
**为什么需要**：编译器需要处理命令行选项
- [ ] 参数解析库
- [ ] 选项定义（-o, --output, --verbose 等）
- [ ] 帮助信息生成
- [ ] 错误处理

### 20. 进程调用接口
**为什么需要**：调用 clang、链接器等外部工具
- [ ] `exec(cmd: string, args: list[string]) -> int`
- [ ] 捕获标准输出
- [ ] 捕获标准错误
- [ ] 进程退出码检查

### 21. 测试框架
**为什么需要**：确保编译器正确性
- [ ] 测试断言函数
- [ ] 测试运行器
- [ ] 词法分析器测试
- [ ] 语法分析器测试
- [ ] 类型检查器测试
- [ ] 端到端测试

### 22. 标准库增强
**为什么需要**：通用编程需求
- [ ] 数学函数（abs, min, max, pow 等）
- [ ] 列表操作（map, filter, reduce, sort 等）
- [ ] 集合类型（Set）
- [ ] 正则表达式（可选）
- [ ] JSON 解析（可选）

---

## 第六阶段：自举验证（P5 - 最终目标）

### 23. 自举编译
**里程碑**：用 Dream 编译器编译 Dream 编译器本身
- [ ] 用现有的 OCaml 编译器编译 Dream 版编译器 → `dream-compiler-v1`
- [ ] 用 `dream-compiler-v1` 编译 Dream 编译器源码 → `dream-compiler-v2`
- [ ] 验证 `dream-compiler-v2` 能再次编译自己 → `dream-compiler-v3`
- [ ] 比较 v2 和 v3 的二进制是否一致（bit-for-bit reproduction）

### 24. 性能优化
**自举后的优化**：
- [ ] 编译速度优化
- [ ] 内存占用优化
- [ ] 生成代码质量优化
- [ ] 并行编译支持

### 25. 工具链完善
**成为完整的语言**：
- [ ] REPL（交互式解释器）
- [ ] 包管理器
- [ ] 格式化工具（formatter）
- [ ] 语法检查工具（linter）
- [ ] LSP 服务器（IDE 支持）
- [ ] 文档生成器

---

## 关键技术挑战

### 1. 循环依赖问题
**问题**：编译器需要字典，但字典需要泛型，泛型需要编译器支持
**解决方案**：
- 先用简单的字典实现（固定类型）
- 再逐步添加泛型支持
- 最后用泛型重写字典

### 2. 内存管理
**问题**：编译器会产生大量临时对象
**解决方案**：
- 依赖已有的引用计数 + GC
- 针对编译器特点优化内存池
- 考虑 Arena 分配器

### 3. 错误恢复
**问题**：编译错误后如何继续解析
**解决方案**：
- 实现同步点（synchronization points）
- panic mode 恢复
- 收集所有错误而不是第一个错误就停止

### 4. FFI（外部函数接口）
**问题**：需要调用 C 库（文件 I/O、系统调用）
**解决方案**：
- 实现简单的 C FFI 机制
- 或用 LLVM IR 直接调用 libc 函数
- 先实现关键的几个函数（open, read, write, close）

---

## 时间估算

### 保守估算（按周计）
- 第一阶段（底层基础）：3-4 周
- 第二阶段（核心特性）：4-5 周
- 第三阶段（高级特性）：3-4 周
- 第四阶段（编译器重写）：6-8 周
- 第五阶段（标准库）：2-3 周
- 第六阶段（自举验证）：1-2 周

**总计：19-26 周（约 5-6 个月）**

### 快速路径（最小化实现）
如果只实现自举必需的最小功能集：
- 跳过一些高级特性（闭包、复杂的 OOP）
- 使用简化的模块系统
- 最小化标准库

**快速路径：12-16 周（约 3-4 个月）**

---

## 下一步行动

### 立即可以开始的任务（P0）：
1. **字符串操作增强** - 不依赖其他功能，可立即实现
2. **文件 I/O** - 通过 C FFI 实现，优先级最高
3. **元组类型** - 相对独立，其他功能会依赖它
4. **字典类型** - 核心数据结构，尽早实现

### 推荐顺序：
```
元组 → 字典（简化版） → 文件 I/O → 字符串操作 →
枚举 → Result/Error 处理 → 模式匹配 →
字典（泛型版） → 结构体 → 泛型系统 →
开始重写 Lexer
```

---

## 成功标准

### 自举成功定义：
1. ✅ Dream 编译器完全用 Dream 编写
2. ✅ 能用 Dream 编译器编译自己
3. ✅ 编译结果能通过所有测试用例
4. ✅ v2 和 v3 版本的二进制完全一致
5. ✅ 性能与 OCaml 版本相当（或在可接受范围内）

### 验收测试：
- [ ] 所有现有的 .dm 示例程序都能用新编译器编译
- [ ] 生成的可执行文件行为与 OCaml 版本一致
- [ ] 编译速度测试
- [ ] 内存占用测试
- [ ] 完整的回归测试套件通过
