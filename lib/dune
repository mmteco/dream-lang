(include_subdirs unqualified)

(library
 (name dream_lib)
 (modules ast error types env lexer parser exhaustiveness typeck monomorphize
          cg_types cg_utils cg_shared cg_expr cg_stmt cg_toplevel llvmgen
          symbol_analyzer))

(menhir
 (modules parser))

(ocamllex
 (modules lexer))
